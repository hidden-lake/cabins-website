name = "cabins-checkout"
main = "src/index.js"
compatibility_date = "2026-07-01"

# Secrets (set with `npx wrangler secret put <NAME>`, never committed):
#   STRIPE_SECRET_KEY - restricted key, see README.md
